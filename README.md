# Site Asset Icon Generator

## Purpose
A lightweight HTML/JavaScript tool for creating consistent site asset icons for SharePoint-style navigation, tiles, and related UI use cases. This application uses SVG markup to avoid any additional dependencies but the more icons make the code longer. 

## Features
- Live icon preview
- Accent color selection
- Export to SVG
- Export to PNG
- Multiple icon styles and layouts
- Example tile preview
- Usage notes panel
- Allows for SVG and JSON Icon Packs

## Project Structure
- `IconBuilder.html` - main application
- `css/` - styles
- `js/` - application logic
- `assets/` - screenshots, examples, and sample icons
- `docs/` - usage and design notes
- `archive/` - older versions and backups

## Usage
1. Open `IconBuilder.html` in a browser.
2. Choose icon settings.
3. Preview the result.
4. Export as SVG or PNG.

## Notes
This repository is intended to be safe for public sharing. Do not include internal environment references, private branding, or sensitive screenshots.

## Roadmap
- Split large single-file builds into modular CSS/JS
- Add more sample presets
